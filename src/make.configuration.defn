# make.configuration.defn file for thorn yaml-cpp

# Define standard yaml-cpp utilities and install those that we could build/that are
# present in the system
STD_YAML_CPP_UTILS = 
ALL_UTILS += $(STD_YAML_CPP_UTILS)
